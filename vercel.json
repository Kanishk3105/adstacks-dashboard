{
  "buildCommand": "bash -lc 'export PATH=/tmp/flutter/bin:$PATH && flutter build web --release'",
  "outputDirectory": "build/web",
  "framework": null,
  "installCommand": "bash -lc 'git clone https://github.com/flutter/flutter.git --depth 1 /tmp/flutter && export PATH=/tmp/flutter/bin:$PATH && flutter config --enable-web && flutter pub get'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
